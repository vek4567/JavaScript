let resultArray = []; // Создаем пустой массив
let first = 1;
let second = 2;
let senseOfLife = 42;

resultArray.push(first); // Добавляем значение переменной first в конец массива
resultArray.push(second); // Добавляем значение переменной second в конец массива

resultArray.unshift(senseOfLife); // Добавляем значение переменной senseOfLife в начало массива

console.log(resultArray); // Выводим массив resultArray в консоль
